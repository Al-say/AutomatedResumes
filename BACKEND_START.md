# 🚀 后端启动指南

## 快速启动

项目已配置好，直接运行以下命令启动后端：

### 方式1：使用Gradle启动（推荐）
```bash
# Windows PowerShell
.\gradlew bootRun

# Windows CMD
gradlew bootRun

# Linux/Mac
./gradlew bootRun
```

### 方式2：使用启动脚本
```bash
# Windows
run-backend.bat

# Linux/Mac
./run-backend.sh
```

## 环境验证

✅ **Java版本**: OpenJDK 23.0.2（已安装）
✅ **项目类型**: Spring Boot 3.5.7
✅ **构建工具**: Gradle（Wrapper模式）

## 启动日志

启动成功后，你会看到类似的日志：
```
  .   ____          _            __ _ _
 /\\ / ___'_ __ _ _(_)_ __  __ _ \ \ \ \
( ( )\___ | '_ | '_| | '_ \/ _` | \ \ \ \
 \\/  ___)| |_)| | | | | || (_| |  ) ) ) )
  '  |____| .__|_| |_|_| |_|\__, | / / / /
 =========|_|==============|___/=/_/_/_/
 :: Spring Boot ::                (v3.5.7)
```

## 默认访问地址

- **Web服务**: http://localhost:8080/
- **后端API**: http://localhost:8080/api/
- **静态资源**: http://localhost:8080/dist/ (若已编译前端)

## 常见问题

### 1. 找不到Gradle
确保在项目根目录运行，项目已包含Gradle Wrapper。

### 2. 内存不足
可以增加JVM内存：
```bash
set JAVA_OPTS=-Xmx2g -Xms512m
.\gradlew bootRun
```

### 3. 端口被占用
修改 `src/main/resources/application.yaml` 中的端口配置：
```yaml
server:
  port: 8081  # 改为其他端口
```

## 停止服务

在启动终端按 `Ctrl+C` 即可停止服务。
